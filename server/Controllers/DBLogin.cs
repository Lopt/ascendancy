using @base.model;
using server.Models;
using System;
using System.Collections.Concurrent;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;
using System.Web;

namespace server.Controllers
{
    public class DBLogin
    {
/*        public async Task<List<User>> GetUserLoginID(string ID)
        {
            Guid UserGUID = Guid.NewGuid();
            Account account = new Account(UserGUID);

            UserData db = new UserData();
            var query = from c in db.User orderby c.ID ascending select c;

            List<User> data = query.ToList();
            return data;
        }*/
    }
}